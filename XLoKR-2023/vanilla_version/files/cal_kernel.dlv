
%c represents complements of kernels
%k stands for kernel
%pk means possibly in or out of kernel

% Positions
pos(X) :- e(X,_).
pos(X) :- e(_,X).

% Win_Move
c(X) :- e(X,Y),k(Y).
k(X) :- pos(X), not c(X).
pk(X) :- pos(X), not c(X), not k(X).
